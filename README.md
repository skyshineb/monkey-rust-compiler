# monkey-rust-compiler

Monkey language compiler + VM implementation in Rust.

This project follows compatibility-driven development using `PROTOCOL.md` and `COMPATIBILITY.md` as behavior contracts.

Current status: **Step 1 skeleton only** (project scaffold and module layout; core language logic not implemented yet).
